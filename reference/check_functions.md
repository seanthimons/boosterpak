# Check materialized helper functions for drift

Check materialized helper functions for drift

## Usage

``` r
check_functions(root = ".", verbose = NULL)
```

## Arguments

- root:

  Project root.

- verbose:

  Whether to print a routine summary.

## Value

A data frame with drift status, invisibly.
