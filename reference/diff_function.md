# Diff a materialized helper function against the package version

Diff a materialized helper function against the package version

## Usage

``` r
diff_function(name, root = ".", verbose = NULL)
```

## Arguments

- name:

  Function catalog name.

- root:

  Project root.

- verbose:

  Whether to print the diff.

## Value

Character vector containing a simple line diff, invisibly.
