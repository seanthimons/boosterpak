% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deinit.R
\name{deinit}
\alias{deinit}
\title{Safely deinitialize a boosterpak project}
\usage{
deinit(
  root = ".",
  renv = TRUE,
  boosters = TRUE,
  rprofile = TRUE,
  lockfile = TRUE,
  air = TRUE,
  force = FALSE,
  verbose = NULL
)
}
\arguments{
\item{root}{Existing project root.}

\item{renv}{Whether to remove the project-local \verb{renv/} directory.}

\item{boosters}{Whether to remove \code{boosters.toml} and \verb{boosters/}.}

\item{rprofile}{Whether to remove boosterpak-managed \code{.Rprofile} content.}

\item{lockfile}{Whether to remove \code{renv.lock}.}

\item{air}{Whether to remove an unchanged boosterpak-generated \code{air.toml}.}

\item{force}{Whether to skip interactive selection and confirmation.}

\item{verbose}{Whether to print routine summaries.}
}
\value{
An invisible list with normalized \code{root}, actionable \code{selected}
components, successfully \code{removed} relative paths, \code{rprofile_changed},
named \code{preserved} reasons, \code{restart_required}, and \code{cancelled}.
}
\description{
Removes selected project-local boosterpak and renv artifacts while
preserving unrelated project files and user-level configuration. An
interactive call with all component arguments omitted offers the actionable
components, preselects all of them, and then confirms the exact cleanup plan.
Supplying any component argument skips the picker but still confirms the
plan. Non-interactive cleanup requires \code{force = TRUE}; genuine no-op calls do
not prompt or require \code{force}.
}
\details{
\code{air.toml} is removed only when its complete line sequence matches a known
boosterpak-generated format. \code{.Rprofile} is never deleted: only recognized
managed startup, repository, and install-policy content is removed, along
with a whole-line renv activation expression when both \code{renv} and \code{rprofile}
are \code{TRUE}. Malformed startup markers and all unrelated profile lines are
preserved.
}
\seealso{
\code{\link[=init]{init()}}
}
