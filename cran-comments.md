## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.
* The remaining local NOTE is the intentionally deferred development-version issue (`0.6.0.9000`). Change to the release version before submission.

## Test environments

* local Debian GNU/Linux 13 (trixie), R 4.5.0

## Downstream dependencies

There are currently no downstream dependencies for this new package.
